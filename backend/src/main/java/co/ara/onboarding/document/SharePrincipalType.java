package co.ara.onboarding.document;

/** Mirrors document_share_principal_ck. {@code principal_id} is a soft reference, typed by this. */
public enum SharePrincipalType {
    CONTACT, USER, DEPARTMENT
}
